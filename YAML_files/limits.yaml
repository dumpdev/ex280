apiVersion: "v1"
kind: "LimitRange"
metadata:
  name: "resource-limits"
spec:
  limits:
    - type: "Container"
      default:
        cpu: "100m"
        memory: "100Mi"
      defaultRequest:
        cpu: "30m"
        memory: "30Mi"
